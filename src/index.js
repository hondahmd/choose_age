import React from 'react';
import {
  View
} from 'react-native';

import Header from './header';

const ChooseAge = () => (
  <View style={{ flex: 1 }}>
    <Header />
  </View>
);

export default ChooseAge;
