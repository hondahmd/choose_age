import React from 'react';
import {
  View
} from 'react-native';

import Header from './header';
import Body from './body';

const ChooseAge = () => (
  <View style={{ flex: 1 }}>
    <Header />
    <Body />
  </View>
);

export default ChooseAge;
